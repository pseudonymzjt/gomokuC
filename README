# Gomoku-C (五子棋 AI)

一个使用 C 语言编写的简易五子棋程序，支持人机对战和人人对战。AI 采用 Alpha-Beta 剪枝算法进行搜索。

## 功能特点

- **游戏模式**：
  - 人人对战
  - 人机对战（玩家执黑/执白）
- **AI 算法**：
  - 基于 Alpha-Beta 剪枝的 Minimax 搜索。
  - 启发式棋盘评估函数（识别连五、活四、死四、活三等棋型）。
  - 相邻位置搜索优化（减少搜索空间）。
- **历史记录**：自动将对局步数记录在 `file.txt` 中。
- **跨平台**：支持 Linux/macOS 以及具有 `gcc` 环境的 Windows。

## 快速开始

### 编译
在终端中运行：
```bash
make
```

### 运行
```bash
./gomoku
```

### 清理编译产物
```bash
make clean
```

## 玩法说明
1. 启动程序后，选择游戏模式。
2. 输入坐标进行落子，格式为 `列号+行号`（例如：`H8` 代表棋盘中心）。
3. AI 思考时会显示计算的节点数量。
4. 任何一方达成五连珠即判定获胜。

## 项目结构
- `gomoku.c`: 游戏核心源代码。
- `Makefile`: 编译配置文件。
- `file.txt`: (运行后生成) 保存对局历史记录。

## 技术细节
- **搜索深度**：默认设置为 6 层（可在代码中修改 `depth` 变量）。
- **坐标系统**：
  - 横向：A-O
  - 纵向：1-15

## 许可证
本项目采用 [MIT License](LICENSE) 开源。